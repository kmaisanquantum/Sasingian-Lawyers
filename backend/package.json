{
  "name": "sasingian-lawyers-api",
  "version": "1.0.0",
  "description": "Legal Practice Management System API — Sasingian Lawyers, Port Moresby PNG",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start":  "node scripts/init-db.js && node server.js",
    "dev":    "nodemon server.js",
    "seed":   "node scripts/init-db.js && node scripts/seed-users.js",
    "build":  "echo Backend build not needed",
    "postinstall": "mkdir -p public && cd ../frontend && npm install && npm run build && cp -r dist/* ../backend/public/"
  },
  "dependencies": {
    "bcryptjs":          "^2.4.3",
    "compression":       "^1.7.4",
    "cors":              "^2.8.5",
    "dotenv":            "^16.3.1",
    "express":           "^4.18.2",
    "express-rate-limit":"^7.1.5",
    "express-validator": "^7.0.1",
    "helmet":            "^7.1.0",
    "jsonwebtoken":      "^9.0.2",
    "morgan":            "^1.10.0",
    "pg":                "^8.11.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
